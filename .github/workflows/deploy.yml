  name: Deploy to EC2

  on:
    push:
      branches: [ main ]

  jobs:
    deploy:
      runs-on: ubuntu-latest
      
      steps:
      - name: Checkout code
      - uses: actions/checkout@v4

      - name: Login Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USER }}/deploy-seven .

      - name: Push Image Docker
        run: docker push ${{ secrets.DOCKER_USER }}/deploy-seven
      
      # - name: Install SSH key
      #   uses: shimataro/ssh-key-action@v2
      #   with:
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     known_hosts: ${{ secrets.KNOWN_HOSTS }}
           
      # - name: Create .env file
      #   run: |
      #     cat <<EOF > .env
      #     DB_ROOT_PASSWORD=${{ secrets.DB_ROOT_PASSWORD }}
      #     DB_NAME=${{ secrets.DB_NAME }}
      #     DATABASE_URL=${{ secrets.DATABASE_URL }}
      #     JWT_SECRET=${{ secrets.JWT_SECRET }}
      #     JWT_EXPIRES_IN=${{ secrets.JWT_EXPIRES_IN }}
      #     DB_PORT=${{ secrets.DB_PORT }}
      #     PORT=${{ secrets.PORT }}
      #     EOF

    
 
      
      # - name: Upload files to EC2
      #   run: |
      #     scp -o StrictHostKeyChecking=no .env ubuntu@${{ secrets.EC2_HOST }}:/home/ubuntu/desafio-backend-seven

      
      
      # # - name: Make deploy.sh executable and run it
      # #   run: |
      # #     ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
      # #     cd /home/ubuntu/desafio-backend-seven
      # #     chmod +x deploy.sh
      # #     ./deploy.sh
      # #     EOF